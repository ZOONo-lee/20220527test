npm install -g nodemon
npm install pm2 -g
//<글로벌 설치이기 때문에 보이지 않을 수 있음
//nodemon은 디플로이할땐 사용 x 개발할땐 o

npm install --save path
npm install express
npm install morgan          //로그
npm install body-parser     //post

//비동기적인것을 순차적으로 할 때>
npm install fxjs
npm install ramde